{
  "name": "@teei/contracts",
  "version": "1.0.0",
  "description": "Contract testing suite for TEEI CSR Platform microservices",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:pact": "vitest run --config vitest.pact.config.ts",
    "pact:publish": "pact-broker publish ./pacts --consumer-app-version=$npm_package_version --broker-base-url=$PACT_BROKER_BASE_URL"
  },
  "keywords": ["pact", "contract-testing", "api"],
  "author": "TEEI Platform Team",
  "license": "MIT",
  "devDependencies": {
    "@pact-foundation/pact": "^13.1.0",
    "@types/node": "^20.10.0",
    "vitest": "^1.0.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}
