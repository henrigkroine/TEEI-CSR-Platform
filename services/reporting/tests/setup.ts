/**
 * Test setup file for reporting service tests
 * Required by vitest workspace config
 */

// Add any global test setup here if needed
// For now, this file just needs to exist to satisfy the vitest config

export {};
