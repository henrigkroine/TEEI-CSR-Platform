<!DOCTYPE html>
<html lang="{{pack.metadata.language}}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Regulatory Compliance Pack</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Helvetica Neue', Arial, sans-serif;
      font-size: 11pt;
      line-height: 1.6;
      color: #1f2937;
    }

    .page {
      page-break-after: always;
      padding: 20px;
    }

    .page:last-child {
      page-break-after: auto;
    }

    /* Cover Page */
    .cover-page {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      text-align: center;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
    }

    .cover-page h1 {
      font-size: 36pt;
      margin-bottom: 20px;
      font-weight: 700;
    }

    .cover-page .subtitle {
      font-size: 18pt;
      margin-bottom: 40px;
      opacity: 0.9;
    }

    .cover-page .metadata {
      font-size: 12pt;
      opacity: 0.8;
    }

    .framework-badges {
      display: flex;
      gap: 15px;
      margin: 30px 0;
      justify-content: center;
    }

    .framework-badge {
      padding: 10px 20px;
      border-radius: 8px;
      font-weight: 600;
      font-size: 14pt;
      background: rgba(255, 255, 255, 0.2);
      border: 2px solid rgba(255, 255, 255, 0.5);
    }

    /* Executive Summary */
    .summary-page {
      padding: 40px;
    }

    .summary-page h2 {
      font-size: 24pt;
      margin-bottom: 30px;
      color: #1f2937;
      border-bottom: 3px solid #667eea;
      padding-bottom: 10px;
    }

    .summary-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
      margin: 30px 0;
    }

    .summary-card {
      padding: 20px;
      border-radius: 8px;
      border: 1px solid #e5e7eb;
      background: #f9fafb;
    }

    .summary-card h3 {
      font-size: 14pt;
      color: #6b7280;
      margin-bottom: 10px;
    }

    .summary-card .value {
      font-size: 32pt;
      font-weight: 700;
      color: #1f2937;
    }

    .completeness-bar {
      height: 30px;
      background: #e5e7eb;
      border-radius: 15px;
      overflow: hidden;
      margin: 20px 0;
    }

    .completeness-fill {
      height: 100%;
      background: linear-gradient(90deg, #10b981 0%, #059669 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-weight: 600;
    }

    /* Table of Contents */
    .toc-page {
      padding: 40px;
    }

    .toc-page h2 {
      font-size: 24pt;
      margin-bottom: 30px;
      color: #1f2937;
    }

    .toc-entry {
      display: flex;
      justify-content: space-between;
      padding: 10px 0;
      border-bottom: 1px dotted #d1d5db;
    }

    .toc-entry.level-0 {
      font-weight: 600;
      font-size: 12pt;
      margin-top: 20px;
    }

    .toc-entry.level-1 {
      padding-left: 30px;
      font-size: 11pt;
    }

    /* Sections */
    .section-page {
      padding: 40px;
    }

    .section-header {
      margin-bottom: 30px;
      padding-bottom: 15px;
      border-bottom: 2px solid #e5e7eb;
    }

    .section-header h2 {
      font-size: 20pt;
      color: #1f2937;
      margin-bottom: 10px;
    }

    .section-header .framework-label {
      display: inline-block;
      padding: 5px 15px;
      border-radius: 5px;
      font-size: 10pt;
      font-weight: 600;
      color: white;
      margin-bottom: 10px;
    }

    .status-badge {
      display: inline-block;
      padding: 5px 12px;
      border-radius: 5px;
      font-size: 9pt;
      font-weight: 600;
      text-transform: uppercase;
    }

    .status-badge.complete { background: #10b981; color: white; }
    .status-badge.partial { background: #f59e0b; color: white; }
    .status-badge.missing { background: #ef4444; color: white; }
    .status-badge.not_applicable { background: #6b7280; color: white; }

    .section-content {
      margin: 30px 0;
    }

    .section-content p {
      margin-bottom: 15px;
      text-align: justify;
    }

    /* Tables */
    table {
      width: 100%;
      border-collapse: collapse;
      margin: 20px 0;
      font-size: 10pt;
    }

    table th {
      background: #f3f4f6;
      padding: 12px;
      text-align: left;
      font-weight: 600;
      border: 1px solid #d1d5db;
    }

    table td {
      padding: 10px 12px;
      border: 1px solid #e5e7eb;
    }

    table tr:nth-child(even) {
      background: #f9fafb;
    }

    /* Citations */
    .citations {
      margin-top: 30px;
      padding: 20px;
      background: #f9fafb;
      border-left: 4px solid #667eea;
      border-radius: 5px;
    }

    .citations h4 {
      font-size: 12pt;
      margin-bottom: 15px;
      color: #374151;
    }

    .citation {
      font-size: 9pt;
      margin-bottom: 10px;
      padding: 8px;
      background: white;
      border-radius: 3px;
      border: 1px solid #e5e7eb;
    }

    .citation-id {
      font-weight: 600;
      color: #667eea;
    }

    .citation-source {
      color: #6b7280;
      font-style: italic;
    }

    /* Gaps Page */
    .gaps-page {
      padding: 40px;
    }

    .gaps-page h2 {
      font-size: 24pt;
      margin-bottom: 30px;
      color: #1f2937;
    }

    .gap-item {
      padding: 20px;
      margin-bottom: 20px;
      border-radius: 8px;
      border-left: 4px solid #ef4444;
      background: #fef2f2;
    }

    .gap-item.critical { border-left-color: #dc2626; background: #fef2f2; }
    .gap-item.high { border-left-color: #f59e0b; background: #fffbeb; }
    .gap-item.medium { border-left-color: #eab308; background: #fefce8; }
    .gap-item.low { border-left-color: #6b7280; background: #f9fafb; }

    .gap-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 10px;
    }

    .gap-title {
      font-weight: 600;
      font-size: 12pt;
    }

    .gap-severity {
      padding: 3px 10px;
      border-radius: 3px;
      font-size: 9pt;
      font-weight: 600;
      text-transform: uppercase;
      background: white;
    }

    .gap-description {
      margin: 10px 0;
      font-size: 10pt;
      color: #374151;
    }

    .gap-action {
      margin-top: 10px;
      padding: 10px;
      background: white;
      border-radius: 5px;
      font-size: 10pt;
    }

    .gap-action strong {
      color: #059669;
    }

    /* Footnotes */
    .footnotes-page {
      padding: 40px;
    }

    .footnotes-page h2 {
      font-size: 20pt;
      margin-bottom: 20px;
      color: #1f2937;
    }

    .footnote-item {
      font-size: 9pt;
      margin-bottom: 10px;
      padding-left: 30px;
      text-indent: -30px;
    }

    .footnote-number {
      font-weight: 600;
      color: #667eea;
    }
  </style>
</head>
<body>
  <!-- Cover Page -->
  <div class="page cover-page">
    <h1>Regulatory Compliance Pack</h1>
    <div class="subtitle">
      {{pack.companyId}}<br>
      {{formatDate pack.period.start}} – {{formatDate pack.period.end}}
    </div>
    <div class="framework-badges">
      {{#each pack.frameworks}}
        <div class="framework-badge">{{this}}</div>
      {{/each}}
    </div>
    <div class="metadata">
      Generated: {{formatDate pack.metadata.generatedAt}}<br>
      Version: {{pack.metadata.version}}<br>
      Evidence Count: {{pack.metadata.evidenceCount}}
    </div>
  </div>

  <!-- Executive Summary -->
  <div class="page summary-page">
    <h2>Executive Summary</h2>
    <div class="summary-grid">
      <div class="summary-card">
        <h3>Total Disclosures</h3>
        <div class="value">{{pack.summary.totalDisclosures}}</div>
      </div>
      <div class="summary-card">
        <h3>Completed</h3>
        <div class="value">{{pack.summary.completedDisclosures}}</div>
      </div>
      <div class="summary-card">
        <h3>Partial</h3>
        <div class="value">{{pack.summary.partialDisclosures}}</div>
      </div>
      <div class="summary-card">
        <h3>Missing</h3>
        <div class="value">{{pack.summary.missingDisclosures}}</div>
      </div>
    </div>

    <h3 style="margin-top: 30px; margin-bottom: 15px;">Overall Completeness</h3>
    <div class="completeness-bar">
      <div class="completeness-fill" style="width: {{formatPercentage pack.summary.overallCompleteness}};">
        {{formatPercentage pack.summary.overallCompleteness}}
      </div>
    </div>

    <h3 style="margin-top: 30px; margin-bottom: 15px;">By Framework</h3>
    <table>
      <thead>
        <tr>
          <th>Framework</th>
          <th>Disclosures</th>
          <th>Completeness</th>
        </tr>
      </thead>
      <tbody>
        {{#each pack.summary.byFramework}}
          <tr>
            <td><strong>{{this.framework}}</strong></td>
            <td>{{this.disclosureCount}}</td>
            <td>{{formatPercentage this.completeness}}</td>
          </tr>
        {{/each}}
      </tbody>
    </table>

    {{#if pack.summary.criticalGaps}}
      <div style="margin-top: 30px; padding: 20px; background: #fef2f2; border-left: 4px solid #dc2626; border-radius: 5px;">
        <strong style="color: #dc2626;">⚠️ Critical Gaps: {{pack.summary.criticalGaps}}</strong>
        <p style="margin-top: 10px; font-size: 10pt;">Please review the Data Gaps section for recommended actions.</p>
      </div>
    {{/if}}
  </div>

  <!-- Table of Contents -->
  <div class="page toc-page">
    <h2>Table of Contents</h2>
    {{#each toc}}
      <div class="toc-entry level-{{this.level}}">
        <span>{{this.title}}</span>
        <span>{{this.pageNumber}}</span>
      </div>
    {{/each}}
  </div>

  <!-- Disclosure Sections -->
  {{#each pack.sections}}
    <div class="page section-page">
      <div class="section-header">
        <span class="framework-label" style="background-color: {{frameworkColor this.framework}};">
          {{this.framework}}
        </span>
        <h2>{{this.title}}</h2>
        <span class="status-badge {{this.status}}">{{this.status}}</span>
      </div>

      <div class="section-content">
        {{#if this.content.narrative}}
          <p>{{this.content.narrative}}</p>
        {{/if}}

        {{#if this.content.tables}}
          {{#each this.content.tables}}
            <h4 style="margin-top: 20px;">{{this.title}}</h4>
            <table>
              <thead>
                <tr>
                  {{#each this.headers}}
                    <th>{{this}}</th>
                  {{/each}}
                </tr>
              </thead>
              <tbody>
                {{#each this.rows}}
                  <tr>
                    {{#each this}}
                      <td>{{this}}</td>
                    {{/each}}
                  </tr>
                {{/each}}
              </tbody>
            </table>
          {{/each}}
        {{/if}}
      </div>

      {{#if this.citations.length}}
        <div class="citations">
          <h4>Evidence Citations ({{this.citations.length}})</h4>
          {{#each this.citations}}
            <div class="citation">
              <span class="citation-id">[{{@index}}]</span>
              <span class="citation-source">{{this.source}}</span>
              <span style="color: #9ca3af; font-size: 8pt;"> – ID: {{truncate this.evidenceId 12}}</span>
            </div>
          {{/each}}
        </div>
      {{/if}}
    </div>
  {{/each}}

  <!-- Data Gaps -->
  {{#if pack.gaps.length}}
    <div class="page gaps-page">
      <h2>Data Gaps & Recommendations</h2>
      <p style="margin-bottom: 30px; color: #6b7280;">
        The following data gaps were identified during pack generation. Addressing these gaps will improve compliance completeness.
      </p>

      {{#each pack.gaps}}
        <div class="gap-item {{this.severity}}">
          <div class="gap-header">
            <div class="gap-title">{{this.framework}} – {{this.disclosureId}}: {{this.title}}</div>
            <div class="gap-severity" style="color: {{#if (eq this.severity 'critical')}}#dc2626{{else if (eq this.severity 'high')}}#f59e0b{{else}}#6b7280{{/if}};">
              {{this.severity}}
            </div>
          </div>
          <div class="gap-description">{{this.description}}</div>
          <div class="gap-action">
            <strong>Suggested Action:</strong> {{this.suggestedAction}}
          </div>
        </div>
      {{/each}}
    </div>
  {{/if}}

  <!-- Footnotes -->
  {{#if footnotes.length}}
    <div class="page footnotes-page">
      <h2>Evidence Reference Index</h2>
      {{#each footnotes}}
        <div class="footnote-item">
          <span class="footnote-number">[{{this.id}}]</span>
          {{this.source}} (Evidence ID: {{truncate this.evidenceId 16}}, Relevance: {{formatPercentage this.relevance}})
        </div>
      {{/each}}
    </div>
  {{/if}}
</body>
</html>
