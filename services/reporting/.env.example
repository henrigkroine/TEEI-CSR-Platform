# Server Configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=development
LOG_LEVEL=info

# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/teei_csr_platform
DATABASE_POOL_MIN=2
DATABASE_POOL_MAX=10

# CORS Configuration
CORS_ORIGIN=http://localhost:4321

# Rate Limiting
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=900000

# Email / SMTP Configuration (Required for Scheduled Reports)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=reports@teei.io
SMTP_PASSWORD=your_smtp_password_here
EMAIL_FROM_NAME=TEEI CSR Platform
EMAIL_FROM_ADDRESS=reports@teei.io
EMAIL_MAX_RETRIES=3

# Scheduling Configuration
SCHEDULING_ENABLED=true
SCHEDULING_TIMEZONE=UTC
