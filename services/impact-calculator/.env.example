# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/teei_csr

# API Server Configuration
PORT=3012
HOST=0.0.0.0

# VIS Calculation Configuration
VIS_LAMBDA=0.01                    # Decay rate parameter (0.01 = default)
VIS_ENABLE_DECAY=true              # Enable exponential decay weighting

# Batch Job Configuration
VIS_CRON_SCHEDULE=0 2 * * *        # Cron schedule (default: 2 AM daily)
VIS_RUN_ON_START=false             # Run batch job immediately on start
VIS_VERBOSE=true                   # Enable detailed logging

# Event Listener Configuration
VIS_POLL_INTERVAL=5000             # Poll interval in milliseconds (default: 5000)

# Logging
LOG_LEVEL=info                     # Log level (debug, info, warn, error)
