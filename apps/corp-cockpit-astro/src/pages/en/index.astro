---
// /en/index.astro - Public Marketing Landing Page - PREMIUM HERO

import BaseLayout from '@layouts/BaseLayout.astro';
import '@/styles/premium-cards.css';
---

<BaseLayout title="TEEI CSR Platform - Measure Volunteer Impact" lang="en">
  <!-- FIXED NAVIGATION -->
  <nav class="nav">
    <div class="nav-container">
      <a href="/" class="nav-logo">TEEI</a>
      <div class="nav-links">
        <a href="#programs">Programs</a>
        <a href="#platform">Platform</a>
        <a href="#about">About</a>
      </div>
      <a href="/en/contact" class="nav-cta">Get a Demo</a>
    </div>
  </nav>

  <!-- PREMIUM HERO SECTION -->
  <section class="hero">
    <div class="hero-content animate-on-scroll">
      <p class="hero-eyebrow">THE EDUCATIONAL EQUALITY INSTITUTE</p>
      <h1>
        Your volunteers <span class="accent">change lives.</span><br>
        We <span class="accent">prove it.</span>
      </h1>
      <p class="hero-subtitle">
        TEEI connects your employees with refugees through mentoring, language practice, and integration programs. Track every session. Measure real outcomes. Report to your board with confidence.
      </p>
      <div class="hero-ctas">
        <a href="/en/contact" class="btn btn-primary btn-premium">Schedule a Demo</a>
        <a href="/en/portal" class="btn btn-secondary btn-premium">See the Platform →</a>
      </div>
    </div>

    <!-- Hero Stats with glassmorphism -->
    <div class="hero-stats animate-on-scroll">
      <div class="stat">
        <span class="stat-value counter" data-target="20000" data-suffix="+">0</span>
        <span class="stat-label">Refugees Reached</span>
      </div>
      <div class="stat">
        <span class="stat-value counter" data-target="8000" data-suffix="+">0</span>
        <span class="stat-label">Language Sessions</span>
      </div>
      <div class="stat">
        <span class="stat-value counter" data-target="400" data-suffix="+">0</span>
        <span class="stat-label">Active Mentors</span>
      </div>
      <div class="stat">
        <span class="stat-value">3.5:1</span>
        <span class="stat-label">Social Return on Investment</span>
      </div>
    </div>

    <!-- Scroll Indicator -->
    <div class="scroll-indicator">
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <polyline points="6 9 12 15 18 9"></polyline>
      </svg>
    </div>
  </section>

  <!-- PROBLEM SECTION -->
  <section class="problem-section">
    <div class="container">
      <div class="section-header animate-on-scroll">
        <span class="section-label">THE PROBLEM</span>
        <h2>The Measurement Gap</h2>
        <p class="section-subtitle">Your CSR team knows volunteering matters. Your board wants proof.</p>
      </div>

      <div class="problem-grid">
        <div class="problem-card animate-on-scroll">
          <div class="problem-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M3 3v18h18"/>
              <path d="M18.7 8l-5.1 5.2-2.8-2.7L7 14.3"/>
            </svg>
          </div>
          <h3>"We logged 10,000 hours"</h3>
          <p>Great. But what changed? Without outcome tracking, volunteer hours are feel-good vanity metrics.</p>
        </div>

        <div class="problem-card animate-on-scroll">
          <div class="problem-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"/>
            </svg>
          </div>
          <h3>Data Everywhere</h3>
          <p>Session notes in emails. Hours in spreadsheets. Feedback in Slack. Your impact story is scattered across a dozen systems.</p>
        </div>

        <div class="problem-card animate-on-scroll">
          <div class="problem-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="10"/>
              <path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"/>
              <line x1="12" y1="17" x2="12.01" y2="17"/>
            </svg>
          </div>
          <h3>The Leadership Question</h3>
          <p>"What's our ROI on the CSR budget?" If you can't answer with data, you have a credibility problem.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- SOLUTION SECTION -->
  <section id="programs" class="section section-dark">
    <h2 class="animate-on-scroll">One Platform. Proven Impact.</h2>
    <p class="section-subtitle animate-on-scroll">
      TEEI runs the programs. Our platform proves they work.
    </p>

    <div class="programs-grid">
      <div class="program-card card animate-on-scroll" style="animation-delay: 0ms;">
        <span class="program-icon">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
        </span>
        <h3>Buddy</h3>
        <p>Weekly coffee meetups between employees and refugees. Conversation builds confidence, connection builds integration.</p>
      </div>
      <div class="program-card card animate-on-scroll" style="animation-delay: 100ms;">
        <span class="program-icon">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M7.9 20A9 9 0 1 0 4 16.1L2 22Z"/></svg>
        </span>
        <h3>Language</h3>
        <p>8,000+ learners practicing English, German, and French with your team through video calls. Every session tracked.</p>
      </div>
      <div class="program-card card animate-on-scroll" style="animation-delay: 200ms;">
        <span class="program-icon">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 10v6M2 10l10-5 10 5-10 5z"/><path d="M6 12v5c3 3 9 3 12 0v-5"/></svg>
        </span>
        <h3>Mentorship</h3>
        <p>30-minute career conversations that change trajectories. Your professionals share expertise. Refugees gain direction.</p>
      </div>
      <div class="program-card card animate-on-scroll" style="animation-delay: 300ms;">
        <span class="program-icon">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="22 7 13.5 15.5 8.5 10.5 2 17"/><polyline points="16 7 22 7 22 13"/></svg>
        </span>
        <h3>Upskilling</h3>
        <p>Your experts teach free courses in tech, data, and business skills. Refugees become job-ready.</p>
      </div>
    </div>
  </section>

  <!-- HOW IT WORKS SECTION -->
  <section id="platform" class="how-it-works">
    <div class="container">
      <div class="section-header animate-on-scroll">
        <span class="section-label">THE PROCESS</span>
        <h2>How It Works</h2>
        <p class="section-subtitle">From enrollment to board-ready reports in six simple steps</p>
      </div>

      <div class="steps-grid">
        <div class="step animate-on-scroll">
          <div class="step-number">1</div>
          <h3>Choose Programs</h3>
          <p>Select mentoring, language practice, or buddy matching based on your CSR goals</p>
        </div>

        <div class="step animate-on-scroll">
          <div class="step-number">2</div>
          <h3>Employees Enroll</h3>
          <p>Staff sign up as mentors, language partners, or integration buddies</p>
        </div>

        <div class="step animate-on-scroll">
          <div class="step-number">3</div>
          <h3>Smart Matching</h3>
          <p>Our platform pairs volunteers with refugees based on skills and availability</p>
        </div>

        <div class="step animate-on-scroll">
          <div class="step-number">4</div>
          <h3>Sessions Happen</h3>
          <p>Video calls via Kintell. Every minute automatically tracked</p>
        </div>

        <div class="step animate-on-scroll">
          <div class="step-number">5</div>
          <h3>AI Analysis</h3>
          <p>Feedback transforms into quantified outcome scores with evidence</p>
        </div>

        <div class="step animate-on-scroll">
          <div class="step-number">6</div>
          <h3>Reports Ready</h3>
          <p>SROI calculations, volunteer scores, and full evidence trails</p>
        </div>
      </div>
    </div>
  </section>

  <!-- METRICS SECTION -->
  <section id="about" class="metrics-section">
    <div class="container">
      <div class="section-header animate-on-scroll">
        <span class="section-label">MEASURABLE IMPACT</span>
        <h2>Numbers Leadership Trusts</h2>
        <p class="section-subtitle">Every metric traced back to real evidence. Every claim defensible.</p>
      </div>

      <div class="metrics-grid">
        <div class="metric-card animate-on-scroll">
          <div class="metric-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M12 2v20M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/>
            </svg>
          </div>
          <div class="metric-eyebrow">Social Return on Investment</div>
          <div class="metric-value">3.5:1</div>
          <p class="metric-description">For every $1 invested in volunteer time, $3.50 in measurable social value is created.</p>
          <a href="/en/platform/sroi" class="metric-link">How we calculate SROI →</a>
        </div>

        <div class="metric-card animate-on-scroll">
          <div class="metric-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M22 12h-4l-3 9L9 3l-3 9H2"/>
            </svg>
          </div>
          <div class="metric-eyebrow">Volunteer Impact Score</div>
          <div class="metric-value">0–100</div>
          <p class="metric-description">Each volunteer scored on hours, quality, outcomes, and consistency. Know your top performers.</p>
          <a href="/en/platform/vis" class="metric-link">What VIS measures →</a>
        </div>

        <div class="metric-card animate-on-scroll">
          <div class="metric-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
              <polyline points="14 2 14 8 20 8"/>
              <line x1="16" y1="13" x2="8" y2="13"/>
              <line x1="16" y1="17" x2="8" y2="17"/>
            </svg>
          </div>
          <div class="metric-eyebrow">Evidence Lineage</div>
          <div class="metric-value">100%</div>
          <p class="metric-description">Click any number and trace it back to source. Session notes, feedback, timestamps. Full audit trail.</p>
          <a href="/en/platform/evidence" class="metric-link">Explore evidence trails →</a>
        </div>
      </div>
    </div>
  </section>

  <!-- TESTIMONIAL SECTION -->
  <section class="testimonial-section">
    <div class="container">
      <div class="testimonial-card">
        <div class="testimonial-content">
          <svg class="quote-icon" width="48" height="48" viewBox="0 0 24 24" fill="currentColor">
            <path d="M14.017 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151c-2.432.917-3.995 3.638-3.995 5.849h4v10h-9.983zm-14.017 0v-7.391c0-5.704 3.748-9.57 9-10.609l.996 2.151c-2.433.917-3.996 3.638-3.996 5.849h3.983v10h-9.983z"/>
          </svg>
          <blockquote>
            For the first time, we can show our board exactly what our volunteer program achieved. The SROI numbers changed how leadership views our CSR investment.
          </blockquote>
          <div class="testimonial-attribution">
            <div class="attribution-avatar">
              <span>SJ</span>
            </div>
            <div class="attribution-text">
              <span class="attribution-name">Sarah Jensen</span>
              <span class="attribution-role">VP of Corporate Responsibility</span>
            </div>
          </div>
        </div>
        <div class="testimonial-stats">
          <div class="stat">
            <span class="stat-value">3.8:1</span>
            <span class="stat-label">SROI Achieved</span>
          </div>
          <div class="stat">
            <span class="stat-value">2,400</span>
            <span class="stat-label">Volunteer Hours</span>
          </div>
          <div class="stat">
            <span class="stat-value">89</span>
            <span class="stat-label">Average VIS Score</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA SECTION -->
  <section class="cta-section">
    <div class="cta-background"></div>
    <div class="container">
      <div class="cta-content">
        <span class="cta-label">GET STARTED</span>
        <h2>Ready to prove your impact?</h2>
        <p>Join companies that stopped guessing and started knowing. See your first SROI report in 30 days.</p>
        <div class="cta-buttons">
          <a href="/en/demo" class="btn btn-primary">Schedule a Demo</a>
          <a href="/en/contact" class="btn btn-secondary">Contact Sales</a>
        </div>
        <p class="cta-note">Free pilot program available for qualified organizations</p>
      </div>
    </div>
  </section>

  <!-- TRUST BADGES -->
  <div class="trust-badges">
    <div class="trust-badge">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <rect x="3" y="11" width="18" height="11" rx="2" ry="2"/>
        <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
      </svg>
      <span>GDPR Compliant</span>
    </div>
    <div class="trust-badge">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
        <polyline points="9 12 11 14 15 10"/>
      </svg>
      <span>SOC 2 Certified</span>
    </div>
    <div class="trust-badge">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <circle cx="12" cy="12" r="10"/>
        <path d="M12 6v6l4 2"/>
      </svg>
      <span>501(c)(3) Nonprofit</span>
    </div>
  </div>

  <!-- FOOTER -->
  <footer class="site-footer">
    <div class="container">
      <div class="footer-grid">
        <!-- Brand Column -->
        <div class="footer-brand">
          <a href="/en" class="footer-logo">
            <span class="logo-text">TEEI</span>
          </a>
          <p class="footer-tagline">Equality Through Education</p>
          <p class="footer-description">The Educational Equality Institute connects corporations with refugees through measurable volunteer programs.</p>
          <div class="footer-social">
            <a href="https://linkedin.com/company/teei" aria-label="LinkedIn">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
              </svg>
            </a>
            <a href="https://twitter.com/teikiorg" aria-label="Twitter">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
              </svg>
            </a>
          </div>
        </div>

        <!-- Programs Column -->
        <div class="footer-column">
          <h4>Programs</h4>
          <ul>
            <li><a href="/en/programs/buddy">Buddy Program</a></li>
            <li><a href="/en/programs/language">Language Connect</a></li>
            <li><a href="/en/programs/mentorship">Mentors for Ukraine</a></li>
            <li><a href="/en/programs/upskilling">Upskilling Academy</a></li>
            <li><a href="/en/programs/weei">Women's Entrepreneurship</a></li>
          </ul>
        </div>

        <!-- Platform Column -->
        <div class="footer-column">
          <h4>Platform</h4>
          <ul>
            <li><a href="/en/platform/dashboard">Corporate Cockpit</a></li>
            <li><a href="/en/platform/sroi">SROI Calculator</a></li>
            <li><a href="/en/platform/reports">Impact Reports</a></li>
            <li><a href="/en/platform/integrations">Integrations</a></li>
            <li><a href="/en/docs">Documentation</a></li>
          </ul>
        </div>

        <!-- Company Column -->
        <div class="footer-column">
          <h4>Company</h4>
          <ul>
            <li><a href="/en/about">About TEEI</a></li>
            <li><a href="/en/impact">Our Impact</a></li>
            <li><a href="/en/partners">Partners</a></li>
            <li><a href="/en/careers">Careers</a></li>
            <li><a href="/en/contact">Contact</a></li>
          </ul>
        </div>
      </div>

      <div class="footer-bottom">
        <div class="footer-legal">
          <p>&copy; 2025 The Educational Equality Institute. All rights reserved.</p>
          <nav class="footer-legal-links">
            <a href="/en/privacy">Privacy Policy</a>
            <a href="/en/terms">Terms of Service</a>
            <a href="/en/cookies">Cookie Policy</a>
          </nav>
        </div>
        <div class="footer-certifications">
          <span class="certification">501(c)(3) Nonprofit</span>
          <span class="certification">GDPR Compliant</span>
        </div>
      </div>
    </div>
  </footer>
</BaseLayout>

<style>
  /* Premium Cards imported in frontmatter */
  html { scroll-behavior: smooth; }

  /* GLOBAL: FORCE NO ITALICS ON ANY HEADING */
  h1, h2, h3, h4, h5, h6,
  .section-title,
  [class*="heading"],
  [class*="headline"],
  .hero h1,
  .hero h1 span,
  .hero-headline,
  .hero-headline span,
  .hero-title,
  .hero-title span,
  [class*="hero"] h1,
  [class*="hero"] h1 span,
  .cta h2,
  .cta-section h2,
  .cta-title,
  [class*="cta"] h2 {
    font-style: normal !important;
    -webkit-font-smoothing: antialiased;
  }

  /* Variables */
  :root {
    --space-xs: 0.25rem;
    --space-sm: 0.5rem;
    --space-md: 1rem;
    --space-lg: 2rem;
    --space-xl: 3rem;
    --space-3xl: 5rem;
    --font-heading: 'Lora', Georgia, serif;
    --font-body: 'Roboto Flex', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    --color-primary: #00393f;
    --color-accent: #BA8F5A;
    --radius-lg: 12px;
    --radius-xl: 20px;
    --radius-md: 8px;
    --radius-full: 9999px;
    --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.1);
    --shadow-lg: 0 20px 60px rgba(0, 57, 63, 0.15);
    --text-primary: #00393f;
    --text-body: #475569;
    --text-light: #64748b;
    --border-light: #e2e8f0;
    --bg-light: #f8fafc;
    --bg-white: #ffffff;
    --section-spacing: 5rem;
    --h2-size: 56px;
    --h3-size: 24px;
    --body-large: 20px;
  }

  /* FIXED NAVIGATION WITH BACKDROP BLUR */
  .nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    padding: var(--space-sm) var(--space-xl);
    background: transparent;
    transition: all 0.3s ease;
  }

  .nav.scrolled {
    background: rgba(0, 57, 63, 0.95);
    backdrop-filter: blur(10px);
    box-shadow: var(--shadow-md);
  }

  .nav-container {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .nav-logo {
    font-family: var(--font-heading);
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-accent);
    text-decoration: none;
  }

  .nav-links {
    display: flex;
    gap: var(--space-lg);
  }

  .nav-links a {
    color: white;
    text-decoration: none;
    font-family: var(--font-body);
    font-size: 0.95rem;
    transition: color 0.2s;
  }

  .nav-links a:hover {
    color: var(--color-accent);
  }

  .nav-cta {
    background: var(--color-accent);
    color: var(--color-primary) !important;
    padding: 10px 24px;
    border-radius: var(--radius-full);
    text-decoration: none;
    font-family: var(--font-body);
    font-weight: 600;
    transition: all 0.3s ease;
  }

  .nav-cta:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(186, 143, 90, 0.4);
  }

  /* PREMIUM HERO - FULL 100VH WITH MESH GRADIENT */
  .hero {
    position: relative;
    min-height: 100vh;
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    gap: var(--space-3xl);
    padding: var(--section-spacing) var(--space-xl);
    background: linear-gradient(165deg, #00393f 0%, #004a52 40%, #003840 100%);
    color: white;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse at 30% 20%, rgba(10, 89, 97, 0.4) 0%, transparent 50%),
      radial-gradient(ellipse at 70% 30%, rgba(186, 143, 90, 0.12) 0%, transparent 50%);
    pointer-events: none;
  }

  @media (max-width: 1024px) {
    .hero {
      grid-template-columns: 1fr;
      text-align: center;
      min-height: auto;
      padding: var(--space-3xl) var(--space-lg);
    }

    .hero-stats {
      justify-content: center;
    }
  }

  .hero-content { position: relative; z-index: 1; }

  /* Accent text (gold color) */
  .accent {
    color: var(--color-accent);
  }

  .hero-eyebrow {
    font-size: 13px; letter-spacing: 0.15em;
    text-transform: uppercase; color: #BA8F5A;
    margin-bottom: 24px; font-weight: 700;
    display: inline-block; position: relative;
  }
  .hero-eyebrow::before {
    content: ''; position: absolute;
    left: -40px; top: 50%; width: 30px; height: 2px;
    background: linear-gradient(90deg, transparent, #BA8F5A);
  }

  /* MASSIVE TYPOGRAPHY */
  .hero h1 {
    font-size: 88px; font-weight: 900; line-height: 1.05;
    margin-bottom: 32px; letter-spacing: -0.03em;
    text-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
    color: white !important;
  }

  /* ANIMATED UNDERLINE */
  .underline-animated {
    position: relative;
    display: inline-block;
  }
  .underline-animated::after {
    content: ''; position: absolute;
    bottom: 8px; left: 0; right: 0; height: 8px;
    background: linear-gradient(90deg, #BA8F5A, #d4a66a);
    opacity: 0.3; border-radius: 4px;
    animation: underlineGrow 2s ease infinite;
  }

  @keyframes underlineGrow {
    0%, 100% { transform: scaleX(1); opacity: 0.3; }
    50% { transform: scaleX(1.02); opacity: 0.5; }
  }

  .hero-subtitle {
    font-size: 22px; line-height: 1.6;
    color: rgba(255,255,255,0.9);
    margin-bottom: 48px; max-width: 600px;
    font-weight: 400;
  }

  .hero-ctas { display: flex; gap: 20px; flex-wrap: wrap; }

  /* GLASSMORPHISM STAT CARDS */
  .hero-stats {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-md);
  }

  .stat {
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: var(--radius-xl);
    padding: var(--space-lg);
    text-align: center;
    backdrop-filter: blur(10px);
    transition: all 0.3s ease;
  }

  .stat:hover {
    background: rgba(255, 255, 255, 0.12);
    transform: translateY(-4px);
  }

  .stat-value {
    font-family: var(--font-heading);
    font-size: 2.5rem;
    font-weight: 600;
    color: var(--color-accent);
    display: block;
    margin-bottom: var(--space-xs);
  }

  .stat-label {
    font-family: var(--font-body);
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.8);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  /* SCROLL INDICATOR */
  .scroll-indicator {
    position: absolute; bottom: 40px; left: 50%;
    transform: translateX(-50%);
    color: rgba(255,255,255,0.6);
    cursor: pointer; animation: bounce 2s infinite;
    transition: all 0.3s ease;
    padding: 12px; border-radius: 50%;
    background: rgba(255,255,255,0.05);
    backdrop-filter: blur(8px);
  }
  .scroll-indicator:hover {
    color: #BA8F5A;
    background: rgba(186, 143, 90, 0.1);
    box-shadow: 0 8px 24px rgba(186, 143, 90, 0.2);
  }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateX(-50%) translateY(0);
    }
    40% { transform: translateX(-50%) translateY(-10px); }
    60% { transform: translateX(-50%) translateY(-5px); }
  }

  /* PREMIUM BUTTONS WITH SHIMMER */
  .btn {
    padding: 16px 32px; border-radius: 12px;
    font-weight: 600; font-size: 16px;
    text-decoration: none;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
    display: inline-flex; align-items: center; gap: 10px;
    position: relative; overflow: hidden;
    border: none; cursor: pointer;
  }

  .btn-premium::before {
    content: ''; position: absolute; inset: 0;
    background: linear-gradient(120deg, transparent, rgba(255,255,255,0.3), transparent);
    transform: translateX(-100%);
    transition: transform 0.6s ease;
  }
  .btn-premium:hover::before { transform: translateX(100%); }

  .btn-small { padding: 12px 24px; font-size: 14px; border-radius: 10px; }

  .btn-primary {
    background: linear-gradient(135deg, #BA8F5A 0%, #d4a66a 100%);
    color: #00393f;
    box-shadow:
      0 4px 16px rgba(186, 143, 90, 0.4),
      0 0 0 1px rgba(186, 143, 90, 0.2);
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow:
      0 12px 32px rgba(186, 143, 90, 0.5),
      0 0 0 1px rgba(186, 143, 90, 0.3),
      0 0 60px rgba(186, 143, 90, 0.2);
  }

  .btn-secondary {
    background: rgba(255,255,255,0.05);
    color: white;
    border: 2px solid rgba(255,255,255,0.2);
    backdrop-filter: blur(8px);
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.1);
  }
  .btn-secondary:hover {
    border-color: #BA8F5A;
    background: rgba(186, 143, 90, 0.1);
    transform: translateY(-2px);
    box-shadow: 0 12px 32px rgba(186, 143, 90, 0.2);
  }

  .btn-large { padding: 22px 44px; font-size: 18px; border-radius: 14px; }

  /* SOCIAL PROOF SECTION */
  .social-proof {
    background: var(--bg-light);
    padding: var(--space-xl) var(--space-xl);
    border-bottom: 1px solid var(--border-light);
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .proof-label {
    text-align: center;
    font-family: var(--font-body);
    font-size: 0.875rem;
    color: var(--text-light);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: var(--space-md);
  }

  .logos-grid {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: var(--space-xl);
    flex-wrap: wrap;
  }

  .logo-placeholder {
    color: var(--text-light);
    font-family: var(--font-body);
    font-size: 0.875rem;
    padding: var(--space-sm) var(--space-md);
    border: 1px dashed var(--border-light);
    border-radius: var(--radius-md);
  }

  /* TESTIMONIAL SECTION - Premium redesign */
  .testimonial-section {
    padding: clamp(4rem, 8vw, 6rem) 0;
    background: linear-gradient(180deg, var(--color-primary) 0%, #002a2f 100%);
  }

  .testimonial-section .container {
    max-width: 1000px;
    margin: 0 auto;
    padding: 0 max(1rem, 5%);
  }

  .testimonial-section .testimonial-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 24px;
    padding: 3rem;
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 3rem;
    align-items: center;
  }

  .testimonial-content {
    position: relative;
  }

  .quote-icon {
    color: var(--color-accent);
    opacity: 0.5;
    margin-bottom: 1.5rem;
  }

  .testimonial-section blockquote {
    font-family: var(--font-heading);
    font-size: clamp(1.25rem, 2.5vw, 1.5rem);
    font-weight: 400;
    font-style: normal;
    color: white;
    line-height: 1.6;
    margin: 0 0 2rem 0;
  }

  .testimonial-attribution {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .attribution-avatar {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: var(--color-accent);
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-body);
    font-weight: 600;
    font-size: 0.875rem;
    color: var(--color-primary);
  }

  .attribution-text {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .attribution-name {
    font-family: var(--font-body);
    font-weight: 600;
    font-size: 1rem;
    color: white;
  }

  .attribution-role {
    font-family: var(--font-body);
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.6);
  }

  .testimonial-stats {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    padding-left: 3rem;
    border-left: 1px solid rgba(255, 255, 255, 0.1);
  }

  .testimonial-stats .stat {
    text-align: left;
    background: transparent;
    border: none;
    padding: 0;
  }

  .testimonial-stats .stat-value {
    display: block;
    font-family: var(--font-heading);
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--color-accent);
    line-height: 1;
    margin-bottom: 0.25rem;
  }

  .testimonial-stats .stat-label {
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-weight: 500;
    color: rgba(255, 255, 255, 0.5);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  @media (max-width: 768px) {
    .testimonial-section .testimonial-card {
      grid-template-columns: 1fr;
      padding: 2rem;
    }

    .testimonial-stats {
      flex-direction: row;
      justify-content: space-between;
      padding-left: 0;
      padding-top: 2rem;
      border-left: none;
      border-top: 1px solid rgba(255, 255, 255, 0.1);
    }

    .testimonial-stats .stat {
      text-align: center;
    }
  }

  /* PROBLEM SECTION */
  .problem-section {
    padding: clamp(4rem, 8vw, 6rem) 0;
    background: var(--bg-light);
  }

  .problem-section .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 max(1rem, 5%);
  }

  .problem-section .section-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .problem-section h2 {
    font-family: var(--font-heading);
    font-size: clamp(2rem, 4vw, 2.75rem);
    font-weight: 600;
    font-style: normal;
    color: var(--color-primary);
    margin: 0 0 1rem 0;
  }

  .problem-section .section-subtitle {
    font-family: var(--font-body);
    font-size: 1.125rem;
    color: var(--text-body);
    max-width: 600px;
    margin: 0 auto;
  }

  .problem-section .problem-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }

  .problem-section .problem-card {
    background: white;
    border: 1px solid var(--border-light);
    border-radius: 16px;
    padding: 2rem;
    transition: all 0.3s ease;
  }

  .problem-section .problem-card:hover {
    border-color: var(--color-primary);
    box-shadow: 0 8px 32px rgba(0, 57, 63, 0.08);
    transform: translateY(-4px);
  }

  .problem-icon {
    width: 48px;
    height: 48px;
    border-radius: 12px;
    background: rgba(0, 57, 63, 0.05);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-primary);
    margin-bottom: 1.5rem;
  }

  .problem-section .problem-card h3 {
    font-family: var(--font-heading);
    font-size: 1.25rem;
    font-weight: 600;
    font-style: normal;
    color: var(--color-primary);
    margin: 0 0 0.75rem 0;
  }

  .problem-section .problem-card p {
    font-family: var(--font-body);
    font-size: 0.9375rem;
    color: var(--text-body);
    line-height: 1.6;
    margin: 0;
  }

  @media (max-width: 768px) {
    .problem-section .problem-grid {
      grid-template-columns: 1fr;
    }
  }

  /* SECTIONS */
  .section {
    padding: var(--section-spacing) var(--space-xl);
    position: relative;
  }

  .section h2 {
    font-family: var(--font-heading);
    font-size: var(--h2-size);
    font-weight: 600;
    text-align: center;
    margin-bottom: var(--space-md);
    color: var(--text-primary);
  }

  .section-subtitle {
    text-align: center;
    font-size: var(--body-large);
    color: var(--text-body);
    max-width: 600px;
    margin: 0 auto var(--space-xl);
  }

  .section-light { background: #f8fafc; }
  .section-dark { background: #00393f; color: white; }
  .section-dark h2 { color: white; }
  .section-dark::before {
    content: ''; position: absolute; inset: 0;
    background-image: radial-gradient(rgba(186, 143, 90, 0.06) 1px, transparent 1px);
    background-size: 20px 20px;
    pointer-events: none;
  }
  .section-dark .section-subtitle { color: rgba(255,255,255,0.75); }

  /* HOW IT WORKS SECTION */
  .how-it-works {
    padding: clamp(4rem, 8vw, 8rem) 0;
    background: var(--bg-light);
  }

  .how-it-works .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-label {
    display: inline-block;
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--color-accent);
    margin-bottom: 1rem;
  }

  .how-it-works h2 {
    font-family: var(--font-heading);
    font-size: clamp(2rem, 4vw, 2.75rem);
    font-weight: 600;
    font-style: normal;
    color: var(--color-primary);
    margin: 0 0 1rem 0;
  }

  .how-it-works .section-subtitle {
    font-family: var(--font-body);
    font-size: 1.125rem;
    color: var(--text-body);
    max-width: 600px;
    margin: 0 auto;
  }

  /* METRICS SECTION */
  .metrics-section {
    position: relative;
    padding: clamp(5rem, 10vw, 8rem) 0;
    background: var(--color-primary);
    color: white;
  }

  .metrics-transition {
    position: absolute;
    top: -80px;
    left: 0;
    right: 0;
    height: 80px;
    background: linear-gradient(to bottom,
      transparent 0%,
      rgba(0, 57, 63, 0.03) 30%,
      rgba(0, 57, 63, 0.08) 60%,
      var(--color-primary) 100%
    );
    pointer-events: none;
  }

  .metrics-section .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .metrics-section .section-label {
    color: var(--color-accent);
  }

  .metrics-section h2 {
    font-family: var(--font-heading);
    font-size: clamp(2rem, 4vw, 2.75rem);
    font-weight: 600;
    font-style: normal;
    color: white;
    margin: 0 0 1rem 0;
  }

  .metrics-section .section-subtitle {
    font-family: var(--font-body);
    font-size: 1.125rem;
    color: rgba(255, 255, 255, 0.7);
    max-width: 600px;
    margin: 0 auto;
  }

  /* CTA SECTION - Light background to break up dark sections */
  .cta-section {
    position: relative;
    padding: clamp(5rem, 10vw, 8rem) 0;
    background: var(--bg-light);
    overflow: hidden;
  }

  .cta-background {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background:
      radial-gradient(ellipse at 20% 50%, rgba(0, 57, 63, 0.03) 0%, transparent 50%),
      radial-gradient(ellipse at 80% 50%, rgba(186, 143, 90, 0.05) 0%, transparent 50%);
    pointer-events: none;
  }

  .cta-section .container {
    max-width: 800px;
    margin: 0 auto;
    padding: 0 max(1rem, 5%);
    position: relative;
    z-index: 1;
  }

  .cta-content {
    text-align: center;
  }

  .cta-label {
    display: inline-block;
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--color-accent);
    margin-bottom: 1rem;
  }

  .cta-section h2 {
    font-family: var(--font-heading);
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 600;
    font-style: normal;
    color: var(--color-primary);
    margin: 0 0 1rem 0;
    line-height: 1.2;
  }

  .cta-content > p {
    font-family: var(--font-body);
    font-size: 1.125rem;
    color: var(--text-body);
    margin: 0 0 2rem 0;
    max-width: 500px;
    margin-left: auto;
    margin-right: auto;
  }

  .cta-section .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 1.5rem;
  }

  .cta-section .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 1rem 2rem;
    font-family: var(--font-body);
    font-size: 1rem;
    font-weight: 600;
    text-decoration: none;
    border-radius: 500px;
    transition: all 0.3s ease;
    min-width: 180px;
  }

  .cta-section .btn-primary {
    background: var(--color-primary);
    color: white;
    border: 2px solid var(--color-primary);
  }

  .cta-section .btn-primary:hover {
    background: var(--color-accent);
    border-color: var(--color-accent);
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(186, 143, 90, 0.3);
  }

  .cta-section .btn-secondary {
    background: transparent;
    color: var(--color-primary);
    border: 2px solid var(--color-primary);
  }

  .cta-section .btn-secondary:hover {
    background: var(--color-primary);
    color: white;
    transform: translateY(-2px);
  }

  .cta-note {
    font-family: var(--font-body);
    font-size: 0.875rem;
    color: var(--text-light);
    margin: 0;
  }

  @media (max-width: 480px) {
    .cta-section .cta-buttons {
      flex-direction: column;
      align-items: center;
    }

    .cta-section .btn {
      width: 100%;
      max-width: 280px;
    }
  }

  /* GRIDS */
  .problem-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 32px; max-width: 1200px; margin: 0 auto;
  }

  /* Metrics Grid */
  .metrics-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .metric-card {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    padding: 2rem;
    transition: all 0.3s ease;
  }

  .metric-card:hover {
    background: rgba(255, 255, 255, 0.08);
    border-color: var(--color-accent);
    transform: translateY(-4px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
  }

  .metric-icon {
    width: 56px;
    height: 56px;
    border-radius: 12px;
    background: rgba(186, 143, 90, 0.15);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
    color: var(--color-accent);
  }

  .metric-eyebrow {
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: rgba(255, 255, 255, 0.5);
    margin-bottom: 0.5rem;
  }

  .metric-value {
    font-family: var(--font-heading);
    font-size: clamp(2.5rem, 4vw, 3.5rem);
    font-weight: 700;
    color: var(--color-accent);
    margin-bottom: 0.5rem;
    line-height: 1;
  }

  .metric-label {
    font-family: var(--font-body);
    font-size: 1rem;
    font-weight: 600;
    color: white;
    margin-bottom: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .metric-description {
    font-family: var(--font-body);
    font-size: 0.9375rem;
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.6;
    margin: 0 0 1.5rem 0;
  }

  .metric-link {
    font-family: var(--font-body);
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--color-accent);
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
    transition: all 0.2s ease;
  }

  .metric-link:hover {
    color: white;
    gap: 0.5rem;
  }

  @media (max-width: 1024px) {
    .metrics-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .metric-card:last-child {
      grid-column: span 2;
      max-width: 400px;
      margin: 0 auto;
    }
  }

  @media (max-width: 640px) {
    .metrics-grid {
      grid-template-columns: 1fr;
    }
    .metric-card:last-child {
      grid-column: span 1;
      max-width: none;
    }
  }

  /* Programs grid - 2x2 layout */
  .programs-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 32px; max-width: 1000px; margin: 0 auto;
  }

  @media (max-width: 768px) {
    .programs-grid {
      grid-template-columns: 1fr;
    }
  }

  /* Steps Grid - Horizontal flow with connectors */
  .steps-grid {
    display: grid;
    grid-template-columns: repeat(6, 1fr);
    gap: 0;
    position: relative;
    max-width: 1200px;
    margin: 0 auto;
  }

  /* Connecting line behind all steps */
  .steps-grid::before {
    content: '';
    position: absolute;
    top: 20px;
    left: calc(8.33% + 20px);
    right: calc(8.33% + 20px);
    height: 1px;
    background: linear-gradient(90deg,
      transparent 0%,
      var(--border-light) 10%,
      var(--border-light) 90%,
      transparent 100%
    );
    z-index: 0;
  }

  @media (max-width: 1024px) {
    .steps-grid {
      grid-template-columns: repeat(3, 1fr);
      gap: 2rem 1rem;
    }
    .steps-grid::before {
      display: none;
    }
  }

  @media (max-width: 640px) {
    .steps-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 2rem;
    }
  }

  /* CARDS */
  .problem-card, .program-card {
    background: white; padding: 40px; border-radius: 20px;
    box-shadow:
      0 0 0 1px rgba(0, 57, 63, 0.05),
      0 20px 60px rgba(0, 57, 63, 0.08);
    position: relative;
  }

  /* Metrics cards stay dark with transparent background */
  .metrics-section .metric-card {
    background: rgba(255, 255, 255, 0.05) !important;
    padding: 2rem;
  }
  .card { transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1); }
  .card:hover {
    transform: translateY(-8px);
    box-shadow:
      0 0 0 1px rgba(0, 57, 63, 0.08),
      0 30px 80px rgba(0, 57, 63, 0.12);
  }

  .section-dark .program-card {
    background: rgba(255,255,255,0.08);
    border: 1px solid rgba(255,255,255,0.1);
    color: white;
    box-shadow:
      0 0 0 1px rgba(255, 255, 255, 0.05),
      0 20px 60px rgba(0, 0, 0, 0.2);
  }
  .section-dark .program-card:hover {
    background: rgba(255,255,255,0.12);
    border-color: rgba(186, 143, 90, 0.3);
    box-shadow:
      0 0 0 1px rgba(186, 143, 90, 0.2),
      0 30px 80px rgba(0, 0, 0, 0.3);
  }
  .section-dark .program-card h3 { color: white; }
  .section-dark .program-card p { color: rgba(255,255,255,0.75); }

  .section-accent .metric-card {
    background: rgba(255,255,255,0.08);
    border: 1px solid rgba(255,255,255,0.1);
    box-shadow:
      0 0 0 1px rgba(255, 255, 255, 0.05),
      0 20px 60px rgba(0, 0, 0, 0.2);
  }
  .section-accent .metric-card:hover {
    background: rgba(255,255,255,0.12);
    border-color: rgba(186, 143, 90, 0.3);
  }
  .section-accent .metric-card h3 { color: white; font-size: 32px; }
  .section-accent .metric-card p { color: rgba(255,255,255,0.75); font-size: 18px; }

  .problem-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--space-lg);
    max-width: 1200px;
    margin: 0 auto;
  }

  .problem-card {
    background: var(--bg-white);
    border: 1px solid var(--border-light);
    border-radius: var(--radius-xl);
    padding: var(--space-lg);
    transition: all 0.3s ease;
  }

  .problem-card:hover {
    border-color: var(--color-accent);
    box-shadow: var(--shadow-lg);
    transform: translateY(-4px);
  }

  .problem-card h3 {
    font-family: var(--font-heading);
    font-size: var(--h3-size);
    color: var(--text-primary);
    margin-bottom: var(--space-sm);
  }

  .problem-card p {
    font-family: var(--font-body);
    color: var(--text-body);
    line-height: 1.6;
  }

  .program-card h3 {
    font-size: 24px; margin-bottom: 16px;
    font-weight: 700; color: #00393f;
  }
  .program-card p {
    font-size: 16px; line-height: 1.6; color: #475569;
  }

  .program-icon { display: block; margin-bottom: 16px; color: #BA8F5A; }
  .metric-example {
    display: block; margin-top: 16px; font-size: 14px;
    color: #BA8F5A; font-weight: 500; line-height: 1.5;
  }

  /* STEPS - Premium design */
  .step {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 0 0.75rem;
    position: relative;
    z-index: 1;
  }

  .step-number {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: transparent;
    border: 2px solid var(--color-accent);
    color: var(--color-accent);
    font-family: var(--font-body);
    font-size: 1rem;
    font-weight: 600;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1rem;
    position: relative;
    z-index: 2;
    transition: all 0.3s ease;
  }

  .step:hover .step-number {
    background: var(--color-accent);
    color: white;
    transform: scale(1.1);
  }

  .step h3 {
    font-family: var(--font-heading);
    font-size: 1rem;
    font-weight: 600;
    font-style: normal;
    color: var(--color-primary);
    margin: 0 0 0.375rem 0;
    line-height: 1.3;
  }

  .step p {
    font-family: var(--font-body);
    font-size: 0.8125rem;
    color: var(--text-body);
    line-height: 1.5;
    margin: 0;
    max-width: 140px;
  }

  /* SITE FOOTER - Premium redesign */
  .site-footer {
    background: var(--color-primary);
    color: white;
    padding: 4rem 0 2rem;
  }

  .site-footer .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 max(1rem, 5%);
  }

  .footer-grid {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr 1fr;
    gap: 3rem;
    margin-bottom: 3rem;
  }

  /* Brand column */
  .site-footer .footer-brand {
    padding-right: 2rem;
  }

  .footer-logo {
    display: inline-block;
    text-decoration: none;
    margin-bottom: 1rem;
  }

  .footer-logo .logo-text {
    font-family: var(--font-heading);
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-accent);
  }

  .footer-tagline {
    font-family: var(--font-body);
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--color-accent);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin: 0 0 1rem 0;
  }

  .footer-description {
    font-family: var(--font-body);
    font-size: 0.9375rem;
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.6;
    margin: 0 0 1.5rem 0;
  }

  .footer-social {
    display: flex;
    gap: 1rem;
  }

  .footer-social a {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.1);
    color: white;
    transition: all 0.3s ease;
  }

  .footer-social a:hover {
    background: var(--color-accent);
    color: var(--color-primary);
    transform: translateY(-2px);
  }

  /* Link columns */
  .footer-column h4 {
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-accent);
    margin: 0 0 1.5rem 0;
  }

  .footer-column ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .footer-column li {
    margin-bottom: 0.75rem;
  }

  .footer-column a {
    font-family: var(--font-body);
    font-size: 0.9375rem;
    color: rgba(255, 255, 255, 0.7);
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .footer-column a:hover {
    color: white;
  }

  /* Footer bottom */
  .site-footer .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 2rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
  }

  .footer-legal p {
    font-family: var(--font-body);
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.5);
    margin: 0 0 0.5rem 0;
  }

  .footer-legal-links {
    display: flex;
    gap: 1.5rem;
  }

  .footer-legal-links a {
    font-family: var(--font-body);
    font-size: 0.8125rem;
    color: rgba(255, 255, 255, 0.5);
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .footer-legal-links a:hover {
    color: white;
  }

  .footer-certifications {
    display: flex;
    gap: 1rem;
  }

  .certification {
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-weight: 500;
    color: rgba(255, 255, 255, 0.5);
    padding: 0.5rem 1rem;
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 500px;
  }

  @media (max-width: 1024px) {
    .footer-grid {
      grid-template-columns: 1fr 1fr;
      gap: 2rem;
    }

    .site-footer .footer-brand {
      grid-column: span 2;
      padding-right: 0;
      padding-bottom: 2rem;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
  }

  @media (max-width: 640px) {
    .footer-grid {
      grid-template-columns: 1fr;
    }

    .site-footer .footer-brand {
      grid-column: span 1;
    }

    .site-footer .footer-bottom {
      flex-direction: column;
      gap: 1.5rem;
      text-align: center;
    }

    .footer-legal-links {
      justify-content: center;
    }

    .footer-certifications {
      justify-content: center;
    }
  }

  /* TRUST BADGES */
  .trust-badges {
    display: flex;
    justify-content: center;
    gap: 2rem;
    padding: 2rem 0;
    flex-wrap: wrap;
    background: var(--bg-light);
    border-top: 1px solid var(--border-light);
  }

  .trust-badge {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-body);
    font-size: 0.8125rem;
    font-weight: 500;
    color: var(--text-light);
  }

  .trust-badge svg {
    color: var(--color-accent);
  }

  /* IMPROVED HERO STATS CARDS */
  .hero-stats .stat {
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 16px;
    padding: 1.5rem;
  }

  .hero-stats .stat:hover {
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(186, 143, 90, 0.3);
    transform: translateY(-2px);
  }

  .hero-stats .stat-label {
    font-size: 0.75rem;
    line-height: 1.3;
    white-space: normal;
    word-wrap: break-word;
  }

  /* IMPROVED PROGRAM CARDS */
  .program-icon svg {
    width: 32px;
    height: 32px;
    stroke-width: 1.5;
  }

  .section-dark .program-card:hover {
    transform: translateY(-4px);
    border-color: var(--color-accent);
  }

  /* DELETE GOLD SQUIGGLE DECORATIONS */
  .section h2::before,
  .section-dark h2::before,
  .how-it-works h2::before,
  .metrics-section h2::before,
  .cta-section h2::before,
  .problem-section h2::before,
  .testimonial-section h2::before,
  [class*="section"] h2::before {
    display: none !important;
    content: none !important;
  }

  /* ANIMATIONS */
  .animate-on-scroll {
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 0.6s ease, transform 0.6s ease;
  }

  .animate-on-scroll.visible {
    opacity: 1;
    transform: translateY(0);
  }

  @media (prefers-reduced-motion: reduce) {
    .animate-on-scroll {
      opacity: 1;
      transform: none;
      transition: none;
    }
  }

  /* RESPONSIVE */
  @media (max-width: 1024px) {
    .hero { grid-template-columns: 1fr; gap: 60px; height: auto; }
    .hero h1 { font-size: 64px; }
    .steps-grid { grid-template-columns: repeat(3, 1fr); }
    .nav-links { gap: 20px; }
    .footer-content { grid-template-columns: 1fr 1fr; gap: 40px; }
  }

  @media (max-width: 768px) {
    .hero { min-height: auto; padding: 100px 24px 60px; }
    .hero h1 { font-size: 48px; }
    .hero-subtitle { font-size: 18px; }
    .hero-stats { grid-template-columns: 1fr; gap: 16px; }
    .section { padding: 80px 24px; }
    .section h2 { font-size: 40px; }
    .section-subtitle { font-size: 18px; }
    .problem-grid, .programs-grid, .metrics-grid { grid-template-columns: 1fr; gap: 24px; }
    .problem-card, .program-card, .metric-card { padding: 32px; }
    .footer { padding: 60px 24px 24px; }
    .nav-links { display: none; }
    .btn-large { padding: 18px 36px; font-size: 16px; }
  }

  @media (max-width: 375px) {
    .hero h1 { font-size: 36px; }
    .section h2 { font-size: 32px; }
    .stat-value { font-size: 42px; }
  }

  @media (min-width: 1440px) {
    .section { padding: 140px 40px; }
    .hero, .nav-container, .footer-content,
    .problem-grid, .programs-grid, .steps-grid, .metrics-grid {
      max-width: 1400px; margin-left: auto; margin-right: auto;
    }
  }

  /* REDUCED MOTION */
  @media (prefers-reduced-motion: reduce) {
    *, *::before, *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
    }
    .hero::before, .scroll-indicator,
    .underline-animated::after {
      animation: none !important;
    }
  }
</style>
<script>
  // Nav scroll effect
  window.addEventListener('scroll', () => {
    const nav = document.querySelector('.nav');
    if (nav) {
      if (window.scrollY > 50) {
        nav.classList.add('scrolled');
      } else {
        nav.classList.remove('scrolled');
      }
    }
  });

  // Intersection Observer for fade-in animations
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
      }
    });
  }, observerOptions);

  document.querySelectorAll('.animate-on-scroll').forEach(el => {
    observer.observe(el);
  });

  // Counter animation
  function animateCounter(el: HTMLElement, target: number) {
    let current = 0;
    const increment = target / 40;
    const timer = setInterval(() => {
      current += increment;
      if (current >= target) {
        el.textContent = target.toLocaleString() + (el.dataset.suffix || '');
        clearInterval(timer);
      } else {
        el.textContent = Math.floor(current).toLocaleString() + (el.dataset.suffix || '');
      }
    }, 40);
  }

  const counterObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const counter = entry.target as HTMLElement;
        const target = parseInt(counter.dataset.target || '0');
        if (target > 0) {
          animateCounter(counter, target);
          counterObserver.unobserve(counter);
        }
      }
    });
  }, { threshold: 0.5 });

  document.querySelectorAll('.counter').forEach(counter => {
    counterObserver.observe(counter);
  });

  // Scroll indicator click handler
  const scrollIndicator = document.querySelector('.scroll-indicator');
  if (scrollIndicator) {
    scrollIndicator.addEventListener('click', () => {
      const firstSection = document.querySelector('.section');
      if (firstSection) {
        firstSection.scrollIntoView({ behavior: 'smooth' });
      }
    });
  }

</script>
