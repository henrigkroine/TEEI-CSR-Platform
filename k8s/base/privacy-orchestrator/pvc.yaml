apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: privacy-orchestrator-data
  namespace: teei-production
  labels:
    app: privacy-orchestrator
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: standard-rwo
