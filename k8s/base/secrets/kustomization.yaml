apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# IMPORTANT: These are template secrets with placeholder values
# Do NOT deploy these directly to any environment
# Use overlays with actual values or integrate with Vault/Sealed Secrets

resources:
  - api-gateway-secret.yaml
  - unified-profile-secret.yaml
  - q2q-ai-secret.yaml
  - analytics-secret.yaml
  - discord-bot-secret.yaml
  - corp-cockpit-secret.yaml
  - connectors-secrets.yaml
  - backend-services-secrets.yaml

# These secrets should be patched in overlays
# See k8s/overlays/staging/secrets-patch.yaml for examples
