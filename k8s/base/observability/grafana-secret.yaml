apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin-credentials
  labels:
    app: grafana
    component: observability
    part-of: teei-csr-platform
type: Opaque
stringData:
  # Default password: admin123 (should be changed in production via sealed secrets)
  # For staging: will be overridden by sealed secret
  password: "admin123"
  username: "admin"
