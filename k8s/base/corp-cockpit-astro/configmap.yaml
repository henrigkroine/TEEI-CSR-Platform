apiVersion: v1
kind: ConfigMap
metadata:
  name: teei-corp-cockpit-config
  labels:
    app: teei-corp-cockpit
    component: frontend
    part-of: teei-csr-platform
data:
  # API Gateway URL (internal)
  API_GATEWAY_URL: "http://teei-api-gateway"

  # Feature flags
  ENABLE_SSE: "true"
  ENABLE_PWA: "true"

  # Logging
  LOG_LEVEL: "info"
